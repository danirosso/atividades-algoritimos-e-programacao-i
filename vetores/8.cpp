#include <iostream>
using namespace std;
#define SIZE 30

int main (){
	int even[], odd[], num;

	for (int i = 0; i < SIZE; i++){
		cout << "Insira o valor [" << i << "]: ";
		cin << num;
		if (num % 2 == 0) even[] += num;
		else odd[] += num;
	}

	return 0;
}
